# git
.git/
# build outputs
dist/
*.zip
# runtime artifacts
.env
# platform SDK copies (fetched at runtime from platform)
gamehub-*.js
agent.md
# editor
.vscode/
.idea/
*.log

# dev-only reference art (huge, not shipped)
src/frame-anim-example/
